<?php
//Geral
    if ($viewData == 'voltar') $viewData = 'Voltar';
    if ($viewData == 'entrar') $viewData = 'Entrar';
    if ($viewData == 'continuar') $viewData = 'Continuar';
    if ($viewData == 'proceder') $viewData = 'Proceder';
    if ($viewData == 'cadastra_sucesso') $viewData = 'foi cadastrada com sucesso!';
    if ($viewData == 'realizada_sucesso') $viewData = 'foi realizada com sucesso!';
    if ($viewData == 'retorna_lista') $viewData = 'Retornar a Lista';
    if ($viewData == 'retorna_lista_de') $viewData = 'Retornar a Lista de ';
    if ($viewData == 'de') $viewData = ' de ';
    if ($viewData == 'o') $viewData = 'o ';
    if ($viewData == 'O') $viewData = 'O ';

//cadastro
    if ($viewData == 'email') $viewData = 'E-mail';
    if ($viewData == 'cadastrar') $viewData = 'Cadastrar';
    if ($viewData == 'inclusao') $viewData = 'Inclusão';
    if ($viewData == 'incluir') $viewData = 'Incluir';
    if ($viewData == 'campos_obrigados') $viewData = 'Campos Obrigatórios';
    if ($viewData == 'atualizacao_de') $viewData = 'A atualização de ';


// Login
    if ($viewData == 'code') $viewData = 'pt-br';
    if ($viewData == 'login') $viewData = 'Login';
    if ($viewData == 'esqueceu') $viewData = 'Esqueceu a password? Escolha aqui';
    if ($viewData == 'sem_cadastro') $viewData = 'Se não tem cadastro, faça-o aqui.';
    if ($viewData == 'escolha') $viewData = 'Escolha o $idioma';

// segunda
    if ($viewData == 'autentica') $viewData = 'Autenticação em duas etapas';
    if ($viewData == 'codigo') $viewData = 'Código';
    if ($viewData == 'sob') $viewData = 'O código está sob o número';
    if ($viewData == 'recebendo_email') $viewData = 'Você está recebendo este e-mail, pois está havendo uma tentativa de logar no site';
    if ($viewData == 'lembrete') $viewData = 'Este é o e-mail do lembrete cadastro';

// recupera a password
    if ($viewData == 'lembrete_password') $viewData = 'Lembrete de sua password';
    if ($viewData == 'recu_password') $viewData = 'Recuperação de password';
    if ($viewData == 'Recu_informe_email') $viewData = 'Informe o e-mail cadastrado para que possamos enviar para este endereço o lembrete da password que foi informado no cadastro';
    if ($viewData == 'Recu_clicando') $viewData = 'Clicando no botão abaixo será direcionado para cadastrar nova password';

// Usuarios
//Create
    if ($viewData == 'password_diferente') $viewData = 'A password e a confirmação são diferentes';
    if ($viewData == 'cadastra_sucesso') $viewData = 'foi cadastrada com sucesso!';
    if ($viewData == '2_campos_devem') $viewData = "Os dois campos acima devem ser preenchidos";

//password
    if ($viewData == 'password') $viewData = 'password';
    if ($viewData == 'password_usu') $viewData = 'password do usuário';
    if ($viewData == 'nova_password') $viewData = 'Nova password';
    if ($viewData == 'password_confirma') $viewData = 'password de confirmação';
    if ($viewData == 'password_repete') $viewData = 'Informe a confirmação da password';
    if ($viewData == 'password_inform') $viewData = 'Informe a nova password';
    if ($viewData == 'cadastra_password') $viewData = 'Cadastrar nova password';

//Usuário Dados 1
    if ($viewData == 'informe') $viewData = 'Informe ';
    if ($viewData == 'o_nome_d') $viewData = 'o Nome d';
    if ($viewData == 'nome_d') $viewData = 'Nome d';
    if ($viewData == 'o_apelido_d') $viewData = 'o Apelido d';
    if ($viewData == 'apelido_d') $viewData = 'Apelido d';
    
    // Post
    if ($viewData == 'o_texto_post_d') $viewData = 'o Texto d';
    if ($viewData == 'texto_post_d') $viewData = 'Texto do Post';
    if ($viewData == 'o_files[]_d') $viewData = 'a/as Mídia(s) d';
    if ($viewData == 'insira') $viewData = 'Insira ';

    
    
    
    
    
    
    
    
    